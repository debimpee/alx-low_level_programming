#include "main.h"

/**
 * *_strcat - concatenates two strings.
 * @dest: the string that appears first
 * @src: the strings that comes after char *dest
 *
 * Return: result
 */
char *_strcat(char *dest, char *src)
{
	_putchar(*dest);
	return (dest);
}
